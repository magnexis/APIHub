# Frontend

This folder now contains the actual React UI source for Magnexis APIHub.

Contents:

- `src/` React app source
- `index.html` Vite entry
- `package.json` frontend launcher/package metadata

Run `npm install` and then `npm run dev` from this folder to start the full desktop app.

The root workspace still owns the Electron shell and backend source files.
